function printUserDetail(userName, age, isMarried) {
  console.log(
    `Your Name Is : ${userName} and your age is : ${age} and also your marital status is : ${isMarried} `
  );
}

let myName = "Abdul Haseeb";

export { printUserDetail, myName };
